{
  "name": "@digital-asset-manager/shared",
  "version": "1.0.0",
  "description": "Shared package containing common code for DAM application.",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "tsc --build --verbose",
    "clean": "rimraf -g dist \"**/*.tsbuildinfo\"",
    "start": "node --env-file=../../.env dist/index.js",
    "prod": "npm run build && npm start"
  },
  "dependencies": {
    "bullmq": "^5.69.3",
    "ioredis": "^5.9.3",
    "minio": "^8.0.6"
  }
}
